# 技术参考与候选集成

本文件记录店策 Agent 后续架构和能力扩展时可复用的外部项目。收录仅表示技术参考，不代表已经集成、官方背书或可以绕过平台权限。

## bububa/oceanengine

- 项目地址：https://github.com/bububa/oceanengine
- 定位：社区维护的巨量引擎 Marketing API Go SDK。
- 覆盖范围：巨量千川、巨量引擎、巨量星图、巨量本地推、企业号和巨量应用市场。
- 千川能力：OAuth2、账户关系、资金、计划、创意、素材、商品、直播、报表、全域推广、关键词和 DMP；同时包含预算、出价、ROI 目标、计划状态等写操作。

### 对店策 Agent 的参考价值

1. 作为未来“官方 API 通道”的接口模型和 Go 实现参考。
2. 为账户、计划、素材、直播和报表建立统一内部数据模型。
3. 在用户具备开放平台资质和授权后，作为浏览器通道之外的可选稳定读取与受控执行层。
4. 参考其广告主 ID、店铺账户和代理商账户关系，完善多千川账号映射。

### 使用边界

- 该 SDK 不能绕过巨量开放平台开发者申请。
- 核心接口仍需要应用凭证、OAuth 授权和 Access Token。
- 当前无 API 用户继续使用已登录网页和浏览器扩展。
- 未完成权限、幂等、审批、限额、审计和操作后验收前，不接入真实写操作。
- 不直接把 Go SDK 打包进浏览器扩展；如采用，应放在独立后端适配层，并转换为店策 Agent 的统一数据协议。

### 推荐演进路线

```text
无 API 用户：已登录千川网页 → 浏览器扩展
有 API 用户：巨量开放平台 → oceanengine 适配服务
                               │
                               ▼
                    统一数据模型与账号目录
                               │
                               ▼
                  分析 → 建议 → 审批 → 受控执行
```

