# 店策 Agent 产品定义（v2.11.0）

## 1. 产品定位

店策 Agent 是抖店商家和千川投手的“本地经营副驾”。它解决的不是“替用户自动点按钮”，而是把散落在抖店、罗盘和千川网页里的信息组织成可验证、可追溯的今日行动清单。

核心价值：

1. 不需要开发者 API，复用用户自己的网页登录态。
2. 不要求运营人员复制 JSON 或导出多个 CSV。
3. 建议必须附数据来源、页面类型和采集时间。
4. 默认不执行写操作并保持脱敏，把错误操作与隐私风险放在产品边界之外。

## 2. 目标用户

- 店铺运营：关心订单、商品、库存、售后、评价和经营趋势。
- 千川投手：关心消耗、成交、ROI、计划状态和素材表现。
- 店长/老板：需要一页看清今天的风险、机会和先后顺序。

## 3. P0 用户闭环

1. 安装扩展并启动本地 Agent。
2. 扩展识别已打开且已登录的抖店/千川页面。
3. 用户点击“同步并诊断”。
4. 系统按页面类型保存脱敏快照并计算质量分。
5. 侧边栏展示今日简报、异常、证据和下一步动作。
6. 对满足安全条件的千川方案，用户可确认或撤销本地操作草稿。
7. 用户回到对应后台核对并人工执行；当前版本不会自动提交。
8. 巡查完成后先查看数据体检单；失败或低质量页面补采完成前，不把相关建议视为可执行结论。
9. 用户选择岗位与当前班次，按标准动作清单完成开播前、直播中或下播后的运营闭环。

## 4. 安全等级

| 等级 | 示例 | 产品策略 |
| --- | --- | --- |
| L0 | 读取、统计、诊断 | 可自动执行 |
| L1 | 标题、话术、计划草稿 | 可生成和复制，不自动提交 |
| L2 | 改库存、发货、回复客户 | 必须展示前后差异并确认 |
| L3 | 改预算、佣金、资金动作 | 二次确认、额度限制、短时审批、审计 |
| L4 | 刷单、伪造交易、绕过风控 | 直接拒绝 |

v2.10.0 在原有 L0/L1 能力上增加 L3 的“操作草稿与本地确认”，但执行层保持硬关闭。预算草稿必须同时绑定用户已选择的千川账号、平台计划唯一 ID、10 分钟内质量分不低于 70 的快照、可回读当前预算以及高置信度诊断；单次增加不超过 15%，单次降低不超过 30%。任一条件不足时只展示诊断和恢复提示，不允许确认。确认和撤销都写入本机审计记录，不会自动点击、提交或修改巨量千川。

## 5. 首版验收标准

- 扩展可在支持 Manifest V3 的 Chromium 浏览器中从解压目录安装；不支持侧栏时使用兼容包和新标签页工作台。
- 权限不包含 `<all_urls>`、cookies 或 debugger。
- 手动同步确实触发所有已打开目标页面采集。
- 同一平台不同页面类型不会互相覆盖。
- 快照包含 schema 版本、来源、页面类型、时间、质量和隐私状态。
- 手机号、证件号、邮箱和订单编号默认脱敏；姓名、地址、账号等敏感列默认排除，隐私模式下页面全文不落盘。
- Companion Service 只监听 127.0.0.1，异常输入不会破坏已有快照。
- 侧边栏在无数据、服务未启动、数据过期和正常状态下都有明确恢复提示。
- 所有诊断包含 evidence 或明确标注为数据覆盖建议。
- 千川建议必须包含计划、消耗/ROI 等依据、建议幅度、置信度和执行护栏。
- 千川预算草稿必须绑定账号与计划唯一 ID，并通过当前值、数据时效、质量分、置信度和幅度上限校验。
- 草稿确认、撤销和查询均可追溯；所有接口明确返回 `execution_enabled: false`。
- 缺少当前预算时必须锁定动作，不得回退为暂停计划或假设计划处于投放中。
- 库存预警区分缺货、极低库存、低库存和预计可售天数。
- 日报按本机时间生成，保存在本地并支持保留天数清理。

## 6. 路线图

### P1：稳定数据模型

- 为订单、商品、售后、计划和报表建立页面 fixture 与选择器回归测试。
- 增加日期范围、店铺/账户识别和跨日快照。
- 支持导出日报与“建议有用/无用”反馈。
- 巡查结果生成账号、覆盖率、质量分、失败原因和单页重试目标组成的数据体检单。
- 岗位与班次模板在浏览器本地保存，并按天记录标准动作完成状态。

### P2：草稿型 Agent

- 从商品页生成标题、卖点和客服话术草稿。
- 对千川调整建议增加多日趋势、素材维度和用户采纳反馈。
- 引入审批单、差异预览和审计记录。

### P3：受控执行

- 仅对明确页面和动作开放 L2/L3 执行。
- 执行前重新读取页面、校验目标、幂等键和审批时效。
- 资金动作默认关闭，必须单独启用。
