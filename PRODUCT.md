# 店策 Agent 产品定义（v4.0.0）

## 1. 产品定位

店策 Agent 是抖店商家和千川投手的“本地经营副驾”。它解决的不是“替用户自动点按钮”，而是把散落在抖店、罗盘和千川网页里的信息组织成可验证、可追溯的今日行动清单。

核心价值：

1. 无 API 用户复用自己的网页登录态；已取得资质的用户可选择官方 OAuth API。
2. 不要求运营人员复制 JSON 或导出多个 CSV。
3. 建议必须附数据来源、页面类型和采集时间。
4. 默认不执行写操作并保持脱敏，把错误操作与隐私风险放在产品边界之外。

### v4.0 发布边界

v4.0 把“产品能运行”和“可以公开发行”拆成两个独立结论：

- `product_operational`：本地 Agent、无 AI 规则诊断、数据仓库、行业知识包和受监督千川执行链路通过本机检查即可为真。
- `public_distribution_ready`：只有生产 Ed25519 公钥已固化，Agent、Updater、安装/升级入口与维护脚本的 Windows Authenticode 全部可核验，目标浏览器商店已发布，而且当前扩展的请求来源、内嵌官方商店扩展 ID、安装来源和目标版本全部一致时才可为真。

前者不能替代后者。当前便携包仍使用无法由 Authenticode 独立证明来源的批处理安装/升级入口，也没有生产签名和商店安装证据，因此只能形成可验收的 4.0 内部发布包。界面和文档必须明确显示“本地可用、公开发行受阻”，不得用测试签名包、开发者模式扩展或只签 Agent 主程序冒充正式发布。

### 抖店服务市场版边界

服务市场版是独立的 `doudian_marketplace/cloud` 部署形态，不是把本地浏览器扩展直接搬到公网：

- 浏览器页面采集、扩展来源上报和 DOM 执行在 HTTP 层强制关闭。
- 商家数据以抖店官方 OAuth、Open API 和消息推送为核心来源。
- 每次请求、Token、快照、任务和报告都必须同时绑定 `tenant_id + shop_id`。
- 本地 `X-Dian-Agent` 请求头不能作为云端认证，云端写操作必须经过正式认证网关。
- 首发只做经营诊断和任务闭环；千川写操作必须取得单独官方权限后再设计。

第一批代码只提供安全模式、适配器接口和发布就绪清单，不宣称企业资质、应用类目、接口权限或安全审核已经通过。具体见 [MARKETPLACE_DEPLOYMENT.md](MARKETPLACE_DEPLOYMENT.md)。

### v4.1 第一批：首价值与今日闭环

- 正式安装的登录启动和周期保活统一通过 `wscript.exe` 隐藏启动器运行，源码开发任务使用独立名称，不再覆盖正式安装任务。
- 工作台显示自动启动、保活、最近健康检查、自动恢复和失败原因；启动状态只读取本机结构化记录。
- 首次使用固定为“环境检查 → 选择店铺 → 同步关键页面 → 生成第一条今日任务 → 查看证据与下一步”，进度保存在本机并可中断续接。
- 首诊断可只采集关键页面；成功时显示“首诊断完成”，但不会冒充 18 页全店巡检已经完整通过。
- 今日任务按“店铺 + 营业日期”隔离，支持开始、转交、阻止、完成和等待复盘，并保留操作者、负责人、原因和时间线。

### v4.1 第二批：纯抖店激活与匿名店铺身份

- 店铺身份与千川账户身份正式拆分。本地 Agent 使用仅保存在本机的随机密钥和 HMAC-SHA256 生成匿名键；快照、目录、任务、价值账本、报告和日志不保存明文店名、账号名称或平台原始 ID。
- 纯抖店用户可以独立完成“环境 → 识别并选择店铺 → 同步经营概览/商品/订单/货架 → 第一条任务 → 证据与下一步”，千川只作为投放分析增强项。
- 只有明确店铺 ID 一致或官方 API 可靠映射才自动建立店铺—账户关系。同名、模糊字段和冲突身份一律不猜，保留为待人工关联。
- 抖店快照、趋势、任务状态、效果基线、价值账本和日报按匿名店铺键隔离；无可靠身份的数据仅保留为未归属兼容快照，不推进激活、不累计价值、不进入受控执行。
- 首页固定回答今日三件事、最大风险、昨日动作结果和未同步数据；价值账本同时汇总任务闭环和受监督执行复盘。

## 2. 目标用户

- 店铺运营：关心订单、商品、库存、售后、评价和经营趋势。
- 千川投手：关心消耗、成交、ROI、计划状态和素材表现。
- 店长/老板：需要一页看清今天的风险、机会和先后顺序。

## 3. P0 用户闭环

1. 安装扩展并启动本地 Agent。
2. 扩展识别已打开且已登录的抖店/千川页面。
3. 用户点击“同步并诊断”。
4. 系统按页面类型保存脱敏快照并计算质量分。
5. 独立经营工作台展示今日简报、异常、证据和下一步动作；点击扩展图标可直接打开或聚焦已有工作台。
6. 对满足安全条件的千川方案，用户可确认或撤销本地操作草稿。
7. 受监督执行器仅对单计划降低预算、恢复已验收的原预算和暂停高风险计划开放；二次确认、短时单次授权、平台成功回执和页面回读缺一不可。
8. 巡查完成后先查看数据体检单；失败或低质量页面补采完成前，不把相关建议视为可执行结论。
9. 用户选择岗位与当前班次，按标准动作清单完成开播前、直播中或下播后的运营闭环。
10. 多千川账户必须先归属到已识别店铺；巡检同时校验匿名店铺键和匿名账户键，任一不一致即停止，页面名称永远不作为放行条件。
11. 用户可声明已在官方千川人工执行确认方案；系统只记录声明，并用声明后的新页面快照核验预算是否达到目标。
12. 数据体检默认突出失败和低质量页面，正常页面折叠收纳；桌面端展开明细时切换整行布局，不阻塞后续今日任务。
13. 用户可直接套用默认经营日报、老板简报或运营交接模板，也可用占位符编辑自定义日志模板。
14. 用户可用飞书或钉钉群自定义机器人 Webhook 接收经营日志；连接步骤固定为“添加机器人 → 粘贴 Webhook → 保存并测试”，无需申请开发者 API。
15. 工作台按经营链路收敛为“货架商品、直播投放、内容”三类；取消总管视图，避免岗位切割和跨模块重复判断。
16. 今日处置队列按紧急程度汇总跨岗位任务，展示真实待处理总数、状态分布和前三项；用户可展开全部，并直接定位到对应经营模块继续处理。
17. 千川建议进入“自动化投放准备度”队列，明确区分可进入执行前检查、等待人工授权、暂时阻止和仅人工处理，并向投手展示阻止原因及补齐路径。
18. 已授权的降低预算方案可启动三分钟受监督执行前检查；必须重新读取当前千川页面并复核账号、计划、预算、质量和降幅，可随时紧急停止。
19. 独立工作台右侧常驻“同步千川”入口；用户只需先访问目标账号的任意千川页面，工作台即可复用最近访问的千川标签页完成单页同步，并保存为后续巡检入口。
20. 已审核的千川开放平台应用可从工作台发起官方 OAuth；App Secret 与 Token 只在本机加密保存，授权失败或 state 不匹配时拒绝连接。
21. 所有经营建议前先展示“当前经营上下文”，统一核对当前店铺、千川账户、数据来源、更新时间和覆盖率；跨店不一致时暂停经营判断。
22. 当前店铺必须由用户明确选择并保存到本地 Agent，浏览器中的历史偏好不得静默替换经营分析账号。
23. 经营工作台默认使用适合长时间运营工作的较大字号；任务正文、数据证据、状态标签和操作按钮不得依赖浏览器缩放才能清晰阅读。

## 4. 安全等级

| 等级 | 示例 | 产品策略 |
| --- | --- | --- |
| L0 | 读取、统计、诊断 | 可自动执行 |
| L1 | 标题、话术、计划草稿 | 可生成和复制，不自动提交 |
| L2 | 改库存、发货、回复客户 | 必须展示前后差异并确认 |
| L3 | 改预算、佣金、资金动作 | 二次确认、额度限制、短时审批、审计 |
| L4 | 刷单、伪造交易、绕过风控 | 直接拒绝 |

v3.1.0 在操作草稿与本地确认之上开放首个 L3 受监督动作：单计划降低预算。预算动作必须绑定用户已选择的千川账号、平台计划唯一 ID、10 分钟内质量分不低于 70 的快照、可回读当前预算以及高置信度诊断；单次降低不超过 30%。执行前重新读取页面，用户输入与目标值绑定的最终确认口令，系统生成 60 秒单次授权；浏览器仅在账号、计划 ID token 完整相等、当前值和唯一提交按钮全部匹配时提交，并在平台成功提示后再次读取页面验收。后续版本只增加“恢复已验收的原预算”和“暂停单个高风险计划”；任意加预算、批量操作、启动计划、出价、库存、订单、退款和资金操作仍保持关闭。

v3.2.0 增加运营级执行额度：默认每个千川账户每天最多执行 3 次、累计预算影响金额不超过 300 元、两次执行至少间隔 30 分钟。额度在启动检查、页面探针和授权消费三个阶段重复校验，避免并发或长时间停留页面绕过限制。

每次受监督执行同时生成 2 小时效果复盘任务，对比调整前后的 ROI 和订单。已经页面验收的降低预算动作可以生成“恢复原预算”方案；系统要求当前预算仍等于上次目标值，并重新走确认、页面回读、短时授权、唯一按钮提交和执行后验收。恢复动作作为安全恢复通道不受普通执行冷却和当日止损额度阻塞，但不能恢复到原记录以外的任意数值。

## 5. 首版验收标准

- 扩展可在支持 Manifest V3 的 Chromium 浏览器中从解压目录安装；所有浏览器统一使用独立标签页工作台。
- 权限不包含 `<all_urls>`、cookies 或 debugger。
- 手动同步确实触发所有已打开目标页面采集。
- 同一平台不同页面类型不会互相覆盖。
- 快照包含 schema 版本、来源、页面类型、时间、质量和隐私状态。
- 手机号、证件号、邮箱和订单编号默认脱敏；姓名、地址、账号等敏感列默认排除，隐私模式下页面全文不落盘。
- 店铺 ID、商家 ID 和账户 ID 只作为本地瞬时身份声明进入解析，HMAC 完成后立即删除；磁盘只保存匿名键。身份密钥随用户配置备份，未恢复密钥时不会按名称猜测历史归属。
- 官方 OAuth 为持续调用平台接口所需的授权元数据位于系统加密凭据文件；不会复制到经营快照、匿名目录、报告或日志。
- 原始千川计划 ID 不落盘；隐私模式保存不包含原文的稳定代号，以支持同一计划的草稿、人工执行声明和回读核验关联。
- Companion Service 只监听 127.0.0.1，异常输入不会破坏已有快照。
- 独立工作台在无数据、服务未启动、数据过期和正常状态下都有明确恢复提示，并在桌面宽屏与窄屏下保持清晰布局。
- 所有诊断包含 evidence 或明确标注为数据覆盖建议。
- 千川建议必须包含计划、消耗/ROI 等依据、建议幅度、置信度和执行护栏。
- 千川预算草稿必须绑定账号与计划唯一 ID，并通过当前值、数据时效、质量分、置信度和幅度上限校验。
- 草稿确认、撤销和查询均可追溯；所有接口明确返回 `execution_enabled: false`。
- 影子执行记录必须明确区分“用户声明人工执行”和“插件执行”；只有声明后的新快照才允许用于结果核验。
- 缺少当前预算时必须锁定动作，不得回退为暂停计划或假设计划处于投放中。
- 库存预警区分缺货、极低库存、低库存和预计可售天数。
- 日报按本机时间生成，保存在本地并支持保留天数清理。
- 日志模板切换不影响原始经营数据，默认模板必须无需配置即可生成。
- 飞书和钉钉 Webhook 只允许官方 HTTPS 地址，完整地址只保存在 `bridge/data/`，读取设置时不得回传密钥。
- 三类工作台只改变任务与模块的业务归属，不删除任务、指标、快照或诊断结果。
- 今日处置队列必须保留任务原有状态与证据；排序和快捷定位只改变展示与导航，不自动执行后台操作。
- 自动化准备度接口只能输出候选资格，必须保持 `execution_enabled: false`；未通过资格闸门的动作不得进入执行前检查。
- 执行前检查只允许降低预算、恢复已验收的原预算和暂停单个高风险计划，禁止自动放量；动作被撤销、会话停止或过期、当前值变化、计划 ID 非完整匹配或任一回读闸门失败时必须阻止授权消费与后续执行。
- OAuth 公网回调不得保存或显示 App Secret、Access Token、Refresh Token；本机状态接口不得回传任何密钥。

## 6. 路线图

### P1：稳定数据模型

- 为订单、商品、售后、计划和报表建立页面 fixture 与选择器回归测试。
- 增加日期范围、店铺/账户识别和跨日快照。
- 支持导出日报与“建议有用/无用”反馈。
- 巡查结果生成账号、覆盖率、质量分、失败原因和单页重试目标组成的数据体检单。
- 岗位与班次模板在浏览器本地保存，并按天记录标准动作完成状态。

### P2：草稿型 Agent

- 从商品页生成标题、卖点和客服话术草稿。
- 对千川调整建议增加多日趋势、素材维度和用户采纳反馈。
- 引入审批单、差异预览和审计记录。
- 引入影子执行、人工操作声明和操作后页面回读核验。

### P3：受控执行

- 以自动化准备度候选队列作为唯一执行入口，禁止绕过资格闸门直接操作页面。
- 仅对明确页面和动作开放 L2/L3 执行。
- 执行前重新读取页面、校验目标、幂等键和审批时效。
- 资金动作默认关闭，必须单独启用。
- 保留“浏览器通道 + 官方 API 通道”的双通道架构；OAuth 授权完成后参考 `bububa/oceanengine` 建立独立数据适配层，仅对已取得开放平台资质和授权的账号启用。

外部项目的定位、采用条件和安全边界见 [技术参考与候选集成](REFERENCES.md)。
